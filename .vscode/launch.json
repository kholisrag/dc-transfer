{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Validate",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/trcli",
      "args": [
        "validate",
        "--transfer",
        "${workspaceFolder}/temp/config.yaml"
      ]
    }
  ]
}